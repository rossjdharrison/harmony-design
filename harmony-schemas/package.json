{
  "name": "@harmony/schemas",
  "version": "1.0.0",
  "description": "JSON schemas and validation utilities for Harmony Design System",
  "type": "module",
  "main": "validate-tokens.js",
  "scripts": {
    "validate": "node validate-tokens.js",
    "validate:tokens": "node validate-tokens.js ../tokens",
    "test": "node validate-tokens.js tokens/colors.example.json && node validate-tokens.js tokens/spacing.example.json"
  },
  "keywords": [
    "json-schema",
    "design-tokens",
    "validation"
  ],
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}